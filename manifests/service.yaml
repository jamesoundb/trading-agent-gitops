apiVersion: v1
kind: Service
metadata:
  name: schwab-mcp
  namespace: trading
spec:
  selector:
    app: schwab-mcp-server
  ports:
  - protocol: TCP
    port: 8000
    targetPort: 8000
  type: ClusterIP
